/**
 * Yetki (permission) tipi.
 * Kanonik tanımlar lib/auth/permissions içindedir; burada yeniden dışa aktarılır.
 */

export { PERMISSIONS } from "@/lib/auth/permissions";
export type { Permission } from "@/lib/auth/permissions";
